# Relaxed requirements for Python 3.12 + CUDA 12.1.
# Pin only where we know the version matters (rectified-flow scheduler in diffusers 0.32+).
# Install torch separately first:
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 --index-url https://download.pytorch.org/whl/cu121

numpy<2.0
scipy
opencv-python
Pillow
matplotlib
h5py
PyYAML
tqdm
trimesh

diffusers>=0.32,<0.34
huggingface-hub
safetensors
transformers<5.0
accelerate

smplx==0.1.28
chumpy @ git+https://github.com/mattloper/chumpy

pyrender
PyOpenGL

joblib
tensorboard
wandb
imageio
